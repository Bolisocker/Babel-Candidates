import { Routes } from '@angular/router';
import { CandidatesPage } from './containers/candidates-page/candidates-page';

const ROUTES: Routes = [{ path: '', component: CandidatesPage }];

export default ROUTES;
